name: Update-Content
run-name: ${{ github.actor }} Running ${{ github.workflow }} on ${{ github.repository }}
on:
  schedule:
    - cron: "0 0 * * 5"
jobs:
  update-content:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "18"
      - run: echo work on progress
